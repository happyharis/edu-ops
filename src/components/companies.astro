---
const templates = [
  {
    title: "Sigma School",
    description: "Help implemented systems, processes and automations. Business grew 6 to 7 figures.",
    image: "src/assets/sigma-school.jpg",
    link: "/startup-template"
  },
  {
    title: "Next: You",
    description: "Get on a free strategy call and we can discuss how we can help",
    image: "src/assets/next-you.jpg",
    link: "/startup-template"
  },
  
];
---
<div class="mt-16">
    <h2 class="text-4xl lg:text-5xl font-bold lg:tracking-tight">
      Companies we have worked with
   </h2>

<div class="grid grid-cols-1 md:grid-cols-2 gap-8">
  {templates.map(template => (
    <div class="mt-8 bg-white rounded-lg shadow-md overflow-hidden">
      <a href={template.link} class="block">
        <img src={template.image} alt={template.title} class="w-full h-48 object-cover" />
      </a>
      <div class="p-4">
        <a href={template.link} class="block">
          <h3 class="text-xl font-bold mb-2">{template.title}</h3>
        </a>
        <p class="text-gray-600">{template.description}</p>
      </div>
    </div>
  ))}
</div>